import { createAction } from "@reduxjs/toolkit";

export const toggleMap = createAction('toggleMap'); 
export const toggleDadata = createAction('toggleDadata'); 
export const addValue = createAction('addValue'); 
export const removeValue = createAction('removeValue'); 
export const editValue = createAction('editValue'); 
export const clearAddress = createAction('clearAddress'); 